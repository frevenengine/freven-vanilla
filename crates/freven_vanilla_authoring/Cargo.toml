[package]
name = "freven_vanilla_authoring"
edition.workspace = true
version.workspace = true
license.workspace = true
publish = false

[dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }
